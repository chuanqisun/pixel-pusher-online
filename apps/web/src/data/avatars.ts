import type { CharacterAtlas } from "assets";
import { alec, ayla, bek, leif, lyster, meg } from "assets";

export const avatars: Record<string, CharacterAtlas> = {
  alec,
  leif,
  meg,
  ayla,
  bek,
  lyster,
};

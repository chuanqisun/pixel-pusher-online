{
  "name": "pixel-pusher-online",
  "version": "0.0.1",
  "description": "Your Figma widget",
  "scripts": {
    "build": "npm run clean && node scripts/build.mjs && node scripts/pack.mjs",
    "dev": "npm run build -- --dev",
    "clean": "rm -rf dist"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "@figma/widget-typings": "*",
    "@types/node": "^16.11.47",
    "esbuild": "^0.14.53",
    "types": "file:packages/types"
  }
}
